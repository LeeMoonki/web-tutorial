/**
 * 문자열을 숫자로 형변환하는 함수입니다. 문자열이 숫자로 변환되면 숫자를 반환하고 그렇지 않으면 null을 반환합니다.
 * @param {string} str 문자열
 */
function stringToNumber(str) {

}

/**
 * 전달받은 인자가 truthy인지 falsy인지 판단하여 truthy면 true를 falsy면 false를 반환합니다.
 * @param {any} value 값
 */
function valueToBoolean(value) {
  
}

module.exports = {
  stringToNumber,
  valueToBoolean
};
